name: Auto Assign

on:
  pull_request:
    types: [opened, reopened, ready_for_review]

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: Auto Assign
        uses: kentaro-m/auto-assign@v1
        with:
          addReviewers: true
          addAssignees: author
          reviewers: |
            WazedKhan
            WazedKhan-Repliq
          numberOfReviewers: 1
          numberOfAssignees: 0
          skipKeywords: |
            wip
            draft
